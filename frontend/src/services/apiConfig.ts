// Aqui ficam guardados os "endereços" (URLs) das APIs.
// Assim, se um dia a URL mudar, só precisamos mudar em UM lugar.

// API em Node.js: vai cuidar do CRUD das ações (criar, listar, editar, apagar)
export const NODE_API_URL = "http://localhost:5000/api";

// API em Python: vai cuidar da geração dos relatórios (PDF, Excel, CSV)
export const PYTHON_API_URL = "http://localhost:8000/api";
